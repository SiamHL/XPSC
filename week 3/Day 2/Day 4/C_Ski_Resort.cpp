#include <bits/stdc++.h>
using namespace std;
void solve(void)
{
    long long int n, k, q;
    cin>>n>>k>>q;
    int l = 0, r = 0, 
     
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    while(t--)
    {
        solve();
    }
    
    
    return 0;
}